{
  "name": "shop-frontend",
  "version": "1.0.0",
  "author": "RIDI",
  "license": "MIT",
  "scripts": {
    "local": "parcel src/index.pug & onchange '**/*.{html,js,json,pug}' -e 'dist/**/*.*' -- prettier --write {{changed}}",
    "build": "rimraf dist & parcel build src/index.pug"
  },
  "devDependencies": {
    "@prettier/plugin-pug": "^1.0.3",
    "less": "^3.10.3",
    "onchange": "^6.1.0",
    "parcel": "^1.12.3",
    "prettier": "^1.18.2",
    "pug": "^2.0.4",
    "rimraf": "^3.0.0"
  },
  "browserslist": [
    "last 2 versions",
    "Android >= 4.2",
    "iOS >= 10",
    "ie > 10"
  ],
  "dependencies": {
    "common": "^0.2.5",
    "jquery": "^3.4.1"
  }
}
